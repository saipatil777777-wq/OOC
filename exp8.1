#include <iostream>
using namespace std;

class Distance {
public:
    int feet, inch;
    Distance(int f, int i) {
        this->feet = f;
        this->inch = i;
    }
    void operator-() {
        feet--;
        inch--;
    }
    void display() {
        cout << "\nFeet & Inches (Decrement): " << feet << " " << inch << endl;
    }
};

int main() {
	
	cout << "Name  : Sai Patil\n";
    cout << "Roll No: 102\n";
    cout << "Class : S.Y.B.CSE\n";
    
	
    Distance d1(8, 9);
    -d1; 
    d1.display();
    return 0;
}
